@{
    ViewData["Title"] = "Admin Dashboard";
}

<div class="container mt-4">
    <h1 class="text-center">@ViewData["Title"]</h1>
    <p class="text-center text-muted">Welcome to the centralized management hub for SmartBudget.</p>

    <div class="d-flex justify-content-center mt-4">
        <a href="@Url.Action("ManageUsers", "Admin")" class="btn btn-primary mx-2">
            <i class="bi bi-people"></i> Manage Users
        </a>
        <a href="@Url.Action("ReviewDataPrivacy", "Admin")" class="btn btn-secondary mx-2">
            <i class="bi bi-shield-lock"></i> Review Data Privacy Compliance
        </a>
    </div>
</div>
